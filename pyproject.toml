[project]
name = "jupyter-template"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.8",
]

[dependency-groups]
dev = [
    "ipykernel>=7.1.0",
    "uv>=0.9.18",
]
